mutation UpdateDocument(
  $id: Int!,
  $value: String!,
  $accessKey: String!,
  $ttlMs: Int!,
  $maxViewCount: Int!
) {
  updateDocument(input: {
    id: $id,
    value: $value,
    accessKey: $accessKey,
    ttlMs: $ttlMs,
    maxViewCount: $maxViewCount
  })
}
